server:
  port: 8764

spring:
  application:
    name: assets

  profiles:
    active: dev

  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://127.0.0.1:8761

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health
  metrics:
    tags:
      application: ${spring.application.name}